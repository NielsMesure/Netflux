

<a href="{{ path('app_film',{'slug' : movie.slug}) }}">
    <div class="container movie_item container-custom text-center">

        <img src="/uploads/{{ movie.illustration }}" alt="{{ movie.name }}" class="img-fluid " style="max-height: 300px; width: auto">
        <div class="middle center">
            <h5>{{ movie.name }}</h5>
        </div>
    </div>
</a>